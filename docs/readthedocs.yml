# See http://read-the-docs.readthedocs.io/en/latest/yaml-config.html

# Build HTML zip, PDF & ePub
formats:
    - htmlzip
    - epub
    - pdf

requirements_file: docs/requirements.txt

python:
   version: 3

# Use the defaults of setup_py_install and pip_install
